# Local dev: use Vite proxy → http://localhost:4000 (see vite.config.js)
VITE_API_BASE_URL=/api

# Razorpay — public key id only (secret is in backend/.env)
VITE_RAZORPAY_KEY_ID=rzp_live_T7nU9HNKNMMLDG